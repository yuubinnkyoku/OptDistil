name: contextual-teacher

on:
  push:
    branches:
      - experiment/contextual-teacher
    paths:
      - "src/optdistil/teachers/meta_attention.py"
      - "src/optdistil/teachers/meta_mlp.py"
      - "src/optdistil/distill/meta_train.py"
      - "scripts/probe_contextual_teacher.py"
      - "scripts/probe_contextual_coupled.py"
      - "tests/test_meta_attention_teacher.py"
      - ".github/workflows/contextual-teacher.yml"
  workflow_dispatch:

concurrency:
  group: contextual-teacher-${{ github.ref }}
  cancel-in-progress: true

jobs:
  compare:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          version: "0.12.10"
          python-version: "3.11"
          enable-cache: true
      - name: Sync CPU environment
        run: uv sync --locked --extra cpu
      - name: Ruff
        run: >-
          uv run --locked --extra cpu ruff check
          src/optdistil/teachers/meta_attention.py
          scripts/probe_contextual_teacher.py
          scripts/probe_contextual_coupled.py
          tests/test_meta_attention_teacher.py
      - name: Contextual-teacher tests
        run: uv run --locked --extra cpu pytest -q tests/test_meta_attention_teacher.py tests/test_meta_teacher.py
      - name: Contextual teacher quick comparison
        run: >-
          uv run --locked --extra cpu python scripts/probe_contextual_teacher.py
          --quick --output artifacts/contextual-teacher.json
      - name: Coupled-task contextual teacher quick comparison
        run: >-
          uv run --locked --extra cpu python scripts/probe_contextual_coupled.py
          --quick --output artifacts/contextual-coupled.json
      - name: Upload comparison metrics
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: contextual-teacher
          path: artifacts/*.json
          if-no-files-found: ignore
